version: '3'

vars:
  FSX_DIR: build/fsx

tasks:
  build:frontend:
    desc: Build the production frontend and verify the release artifacts
    deps:
      - :frontend:test
      - :frontend:build
    cmds:
      - dotnet fsi {{.FSX_DIR}}/VerifyFrontendArtifact.fsx
