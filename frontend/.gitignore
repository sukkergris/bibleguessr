# Dependencies
node_modules

# Build output
dist
dist-ssr

# Artifacts output
artifact

# Vite local env overrides (secrets live in the root .gitignore rules)
*.local

# Playwright test runner output (config lives in frontend/playwright.config.ts)
test-results/
playwright-report/

# Logs
*.log
npm-debug.log*
