name: bibleguessr-build

services:
  api:
    build:
      context: ..
      dockerfile: build/Dockerfile.api
    image: ${DOCKER_USERNAME:?}/bibleguessr-api:${IMAGE_TAG:?}

  nginx:
    build:
      context: ..
      dockerfile: build/Dockerfile.nginx
    image: ${DOCKER_USERNAME:?}/bibleguessr-nginx:${IMAGE_TAG:?}
