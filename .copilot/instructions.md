# Copilot project instructions

- Always start bash scripts with `#!/usr/bin/env bash`.
- Keep script headers consistent and add the shebang as the first line.
- Prefer `bash -n` validation after editing shell scripts.
- Code and documentation should always be in English (US).
