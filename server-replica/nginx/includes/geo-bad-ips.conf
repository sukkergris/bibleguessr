# Same address list as $bad_guys, exposed under a second variable name.
# Includes the bare entry list -- including bad-guys.conf here instead would
# nest a `geo` block inside a `geo` block, which nginx rejects with
# `unexpected "{"`.
geo $is_bad_ip {
    default 0;
    include /etc/nginx/includes/bad-ip-entries.conf;
}
